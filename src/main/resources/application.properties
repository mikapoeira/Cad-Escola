# Configurações da aplicação
spring.application.name=CadEscola MVC
server.port=8080

# Configurações do banco H2 (para desenvolvimento)
spring.datasource.url=jdbc:h2:mem:cadescola
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Console H2 (apenas para desenvolvimento)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.com.cadescola=DEBUG
logging.level.org.springframework.web=DEBUG

# Configurações de produção (descomente para usar PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/cadescola
# spring.datasource.username=postgres
# spring.datasource.password=sua_senha
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
